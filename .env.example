# Base URL of the LLM wrapper service (no trailing slash).
# The app calls POST {LLM_URL}/llm/query
LLM_URL=

# Secret bearer token for the LLM wrapper service.
# Never commit the real value - this is server-side only and is never
# sent to the browser.
LLM_TOKEN=
